const valueDir = "./value";
const logsFile = `${valueDir}/logs.json`;
const serviciesFile = `${valueDir}/servicies.json`;
const LOG_TYPES = ["info", "error"];

module.exports = {
  valueDir,
  logsFile,
  serviciesFile,
  LOG_TYPES,
};
